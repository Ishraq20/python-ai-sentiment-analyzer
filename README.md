# AI Sentiment Analyzer

A simple Python CLI-based program that analyzes user input sentences and predicts whether the sentiment is Positive, Negative, or Neutral based on predefined keywords.

## How to Run
1. Run the `main.py` file using Python or VS Code.
2. Enter any sentence to analyze.
3. Type `exit` to stop the program.# python-ai-sentiment-analyzer
